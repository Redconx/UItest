"use strict";

var _define = require("../preactement/define");
var _Badge = require("../../Badge");
const attributes = ['class-name', 'size', 'variation'];
(0, _define.define)('ds-badge', () => _Badge.Badge, {
  attributes
});