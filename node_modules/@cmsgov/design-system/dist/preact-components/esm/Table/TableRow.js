import { Children, cloneElement } from "preact/compat";
import { jsx as _jsx } from "preact/compat/jsx-runtime";
export const TableRow = _ref => {
  let {
    children,
    _isTableHeadChild,
    ...tableRowProps
  } = _ref;
  const renderChildren = () => {
    return Children.map(children, child => {
      // Extend props before rendering.
      if (child && child.props) {
        return cloneElement(child, {
          _isTableHeadChild: _isTableHeadChild
        });
      }
      return child;
    });
  };
  return /*#__PURE__*/_jsx("tr", {
    role: "row",
    ...tableRowProps,
    children: _isTableHeadChild ? renderChildren() : children
  });
};
export default TableRow;