import { VNode } from 'preact';
type Slots = {
    [k: string]: VNode<any> | string;
};
/**
 * Takes a template element and converts its content into a Preact VNode and also
 * extracts slot-element information.
 */
export declare function templateToPreactVNode(template: HTMLTemplateElement): {
    vnode: VNode;
    slots: Slots;
};
export {};
