import type * as React from 'react';
import { IconCommonProps } from './SvgIcon';
export interface StarIconProps extends IconCommonProps {
    isFilled?: boolean;
}
export declare function StarIcon(props: StarIconProps): React.ReactElement;
