import { ListState, Node, OverlayTriggerState } from '../react-aria';
export interface DropdownMenuOptionProps<T> {
    componentClass: string;
    section: Node<T>;
    state: ListState<T> & OverlayTriggerState;
    rootId?: string;
}
export declare function DropdownMenuSection<T>({ componentClass, section, state, rootId, }: DropdownMenuOptionProps<T>): import("react/jsx-runtime").JSX.Element;
export default DropdownMenuSection;
