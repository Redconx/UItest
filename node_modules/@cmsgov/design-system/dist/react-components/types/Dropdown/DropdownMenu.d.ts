import { RefObject } from 'react';
import type * as React from 'react';
import { ListState, OverlayTriggerState } from '../react-aria';
import { AriaPopoverProps, AriaListBoxOptions } from '../react-aria';
import { DropdownSize } from './Dropdown';
interface DropdownMenuProps<T> extends AriaListBoxOptions<T> {
    children?: React.ReactNode;
    componentClass: string;
    heading?: React.ReactNode;
    labelId: string;
    menuId: string;
    rootId?: string;
    size?: DropdownSize;
    state: ListState<T> & OverlayTriggerState;
    triggerRef: AriaPopoverProps['triggerRef'];
    listBoxRef?: RefObject<any>;
}
export declare function DropdownMenu<T>({ children, componentClass, heading, labelId, menuId, rootId, size, state, ...props }: DropdownMenuProps<T>): import("react/jsx-runtime").JSX.Element;
export default DropdownMenu;
